# LambdaBoiler
A Node Express mySQL boilerplate that runs on AWS Lambda, a serverless function service.

# Packages installed
- npm i serverless-http (lets express run on Lambda serverless)
- npm i express
